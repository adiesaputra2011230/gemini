name: gemini_ai_app
description: A Flutter app with Gemini AI integration.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_generative_ai: ^0.4.3
  flutter_gemini: ^3.0.0
  google_fonts: ^5.0.0
  provider: ^6.0.5
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
